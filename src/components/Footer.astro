<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>HackED 2025</title>
  <style>
    /* Global Reset */
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    /* Full height layout */
    html, body {
      height: 100%;
      display: flex;
      flex-direction: column;
      font-family: "Lexend Deca", sans-serif;
    }

    /* Navigation Bar */
    .navigation {
      background: linear-gradient(45deg, #A3B18A, #80a280);
      display: flex;
      align-items: center;
      justify-content: space-between;
      padding: 15px 20px;
      border-radius: 10px;
      position: relative;
      z-index: 999;
      box-shadow: 3px 3px 10px rgba(0, 0, 0, 0.1);
    }

    .left-nav a {
      font-weight: bold;
      text-decoration: none;
      color: #000;
      font-size: 1.2rem;
    }

    .right-nav {
      display: flex;
      gap: 15px;
      align-items: center;
    }

    .right-nav a {
      font-weight: 350;
      text-decoration: none;
      color: #000;
      padding: 10px;
      transition: transform 0.3s ease;
    }

    .right-nav a:hover {
      transform: scale(1.1);
    }

    /* Hamburger Menu */
    .hamburger {
      display: none;
      font-size: 1.8rem;
      background: none;
      border: none;
      cursor: pointer;
      color: #000;
    }

    /* Mobile Navigation Hidden */
    .right-nav.mobile-hidden {
      display: none;
      flex-direction: column;
      background-color: rgba(83, 83, 83, 0.1);
      position: absolute;
      top: 100%;
      right: 10px;
      border-radius: 10px;
      padding: 10px;
      gap: 10px;
    }

    /* Main Content */
    .main-content {
      flex: 1;
      padding: 20px;
    }

    /* Sticky Footer */
    .footer {
      background: linear-gradient(45deg, #A3B18A, #80a280);
      text-align: center;
      padding: 10px;
      color: rgb(58, 58, 58);
    }

   
  </style>
</head>
<body>



    <button class="hamburger">☰</button>

    <div class="right-nav mobile-hidden">
      <a href="#">Home</a>
      <a href="#">About</a>
      <a href="#">Projects</a>
      <a href="#">Contact</a>
    </div>
  </nav>


  <!-- Footer -->
  <footer class="footer">
     HackED 2025 
  </footer>

  <script>
    const hamburger = document.querySelector('.hamburger');
    const rightNav = document.querySelector('.right-nav');

    hamburger.addEventListener('click', () => {
      rightNav.classList.toggle('mobile-active');
    });
  </script>

</body>
</html>
